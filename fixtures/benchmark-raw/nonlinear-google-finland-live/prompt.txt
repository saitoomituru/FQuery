You are decomposing a short text into FAM JSON (open-world FAM contract: the only universal required keys are ψ, ∇φ, λ, and Q; each axis may be empty or contain nested units that also use the same four axes).

Return ONLY one JSON object (no prose, no markdown fences). Requirements:
- Preserve causal / dependency / motivating relations between the distinct claims in the source as explicit relation entries (e.g. a "relations" array of {"from", "to", "relation"} objects, or nested ∇φ units connected by before/after or depends_on), rather than flattening everything into an unordered list of unrelated facts.
- If the source mixes distinct fact categories (e.g. investment amount, timeframe, physical infrastructure, a separate contractual/energy agreement), represent them as separately addressable units, not one paragraph blob.
- If part of the source references a separate independently-referenceable topic (e.g. a named external agreement with its own parties and duration), you may represent it as its own sub-unit with an explicit reference key (e.g. "fold_ref" or "fam_ref") rather than inlining it as plain prose.
- Mark anything not stated in the source as explicitly unknown (e.g. an "unknowns" array of short labels) rather than inventing or omitting it silently.
- Do not invent a single privileged interpretation when the source supports more than one framing; where relevant, note an alternative framing explicitly (e.g. an "alternative_branches" array).
- This is a business/technology news excerpt. There is no physical tool/actuator action implied; do not invent one.

Source text:
---
Google is making its largest single investment in Europe, committing at least €13 billion, roughly $15.1 billion, to AI infrastructure and energy projects in Finland during 2027 and 2028. The plan covers new data center capacity across four locations, including further expansion of Google's long-running Hamina campus. More unusually, Google signed a 22-year agreement with Finnish utility Fortum to purchase up to half of the electricity generated by the Loviisa nuclear plant between 2030 and 2049.
---

Return the JSON object now.
